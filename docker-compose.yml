version: '3.8'

services:
  career-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: career-web:latest
    container_name: career-web-app
    restart: unless-stopped
    ports:
      - "8000:80"
    networks:
      - career-network

networks:
  career-network:
    driver: bridge